        [-2, 1,-3,4,-1,2,1,-5,4]
        max = -inf
        max = -2
        but if curSum < 0: set curSum = 0
restart       sum to 0
        continue adding
        """
        maxSum = nums[0] # initilize maxSum with -2
        curSum = 0 #initilize

        for num in nums:
        #if we get a negative we just want a 0
            if curSum < 0:
                curSum = 0
            curSum += num
            maxSum = max(maxSum, curSum)   
        return maxSum     