            if char not in hmap:
                stack.append(char)
            else:
                if not stack:
                    return False
                else:
        for char in s:

                    popped = stack.pop()
                    if popped != hmap[char]:
                        return False
        stack = []
        hmap = {')':'(','}':'{',']':'['}
        """
        return not stack             


        so 