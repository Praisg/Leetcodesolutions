class Solution:
    def removeDuplicates(self, nums: List[int]) -> int:
        seen = set()
        p = 0

        for i in range(len(nums)):
            if nums[i] not in seen:
                seen.add(nums[i])
                nums[p] = nums[i]
                p+=1
        return p        