class Solution:
    def hammingWeight(self, n: int) -> int:
        count = 0
        while n:
            # n = (n) & (n-1) #hamming weight
            # count += 1
            count += n & 1
            n //= 2
        return count    