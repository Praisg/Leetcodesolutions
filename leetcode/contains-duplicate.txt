class Solution:
    def containsDuplicate(self, nums: List[int]) -> bool:
       #so im thinking of going thru the assay and adding an element to a set if its not in set and if 
its set we return false
        s = set()
        for i in nums:
            if i in s:
                return True
            s.add(i)

       #[12341]


        return False    

