
        suf [24,12,4,1]

        res[0,0,0,0]
        """
        pref_prod = [1]*n
        suf_prod = [1]*n
        res = [0]*n

        for i in range(1, n):
            pref_prod[i] = pref_prod[i-1]*nums[i-1]

        for j in range(n-2, -1, -1):
            suf_prod[j] = suf_prod[j+1]*nums[j+1]   
        
        for i in range(n):
            res[i] = suf_prod[i]*pref_prod[i]

        return res    