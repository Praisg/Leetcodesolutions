        so we have a decision tree
        which starts empty [] then choose [1] or [2] or [3], if w choose [1,2] or [1,3]
        so we are going to start with a number and if that number is not already part of 
solution we append the number and backtrack on that and pop
        """
        result = []
        solution = []
        def backtrack():
        n = len(nums)
            if len(solution) == n:
                result.append(solution[:])
                return 

            for x in nums:
                if x not in solution:
                    solution.append(x)
                    backtrack()
                    solution.pop()
        backtrack()
        return result               
        """